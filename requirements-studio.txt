# Extra dependency for the parts & assembly studio (`python -m drone_demo
# studio`), pinned to the version installed and smoke-tested in .venv311
# (Python 3.11.7, Windows 11). Not part of requirements.txt: the studio is a
# local, login-gated authoring tool, not part of the published pipeline, so
# the core CAD/CLI workflow's dependency footprint stays untouched without
# this file. Install with:
#
#     .venv311\Scripts\python.exe -m pip install -r requirements.txt -r requirements-studio.txt
#     .venv311\Scripts\python.exe -m pip install -e ".[studio]"

flask==3.1.3
